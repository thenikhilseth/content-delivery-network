/// <reference types="node" />
import http2 from 'http2';
export interface AssetCacheConfig {
    warmupDuration: number;
    promotionRatio: number;
    demotionRatio: number;
    minimumRequests: number;
}
export declare class AssetCache {
    private readonly config;
    private readonly sessionMap;
    private readonly warmingMetrics;
    private readonly assetMap;
    private readonly pushCandidates;
    constructor(config: AssetCacheConfig);
    recordRequestPath(session: http2.Http2Session, path: string, isStatic: boolean): void;
    getAssetsForPath(path: string): Set<string>;
    private onWarm;
}
